// Image imports live here so resume.js stays plain data with no bundler-specific
// imports — that lets Node read it directly to generate the agent-facing files.
import betterlesson from '../assets/betterlesson-logo.png'
import renaissance from '../assets/renaissance-logo.png'
import cmu from '../assets/cmu-logo.png'
import niit from '../assets/niit-university-logo.png'

export const logos = { betterlesson, renaissance, cmu, niit }
